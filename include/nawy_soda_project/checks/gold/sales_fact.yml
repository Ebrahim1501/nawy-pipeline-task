checks for sales_fact:

  - missing_count(id) = 0:
      name: "id col cannot be null"
      fail: when > 0
  - duplicate_count(id) = 0:
      name: "id must be unique"
      fail: when > 0
  - failed rows:
      name: "every sale must have a valid lead_id map"
      warn: when > 0
      fail query: |
        SELECT *
        FROM sales_fact
        WHERE lead_id NOT IN (SELECT lead_id FROM dim_lead_scd2)
  - failed rows:
      name: "every sale must have a valid lead_type_id map"
      warn: when > 0
      fail query: |
        SELECT *
        FROM sales_fact
        WHERE lead_type_id NOT IN (SELECT lead_type_id FROM dim_lead_type)
  - failed rows:
      name: "every sale must have a valid lead_campaign_id map"
      warn: when > 0
      fail query: |
        SELECT *
        FROM sales_fact
        WHERE lead_campaign_id NOT IN (SELECT campaign_id FROM dim_market_campaign)
  - failed rows:
      name: "unit_value must be a non-negative number"
      warn: when > 0
      fail query: |
        SELECT *
        FROM sales_fact
        WHERE unit_value < 0
  - failed rows:
      name: "actual_value must be a  +  number"
      warn: when > 0
      fail query: |
        SELECT *
        FROM sales_fact
        WHERE actual_value < 0
  - failed rows:
      name: "expected_value must be + number"
      warn: when > 0
      fail query: |
        SELECT *
        FROM sales_fact
        WHERE expected_value < 0
  - failed rows:
      name: "years_of_payment must be + number"
      warn: when > 0
      fail query: |
        SELECT *
        FROM sales_fact
        WHERE years_of_payment < 0